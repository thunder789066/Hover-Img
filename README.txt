# Hover-Img
 
Hover cursor over any of the 3 images below main display in order to view thst image bigger as well as the text in 'alt'.

Removing cursor from hovering over the images sets the bigger display to the defaulted image and text. The default image is naturally blank.